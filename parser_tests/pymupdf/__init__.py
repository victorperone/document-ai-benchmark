"""Tests for the PyMuPDF v2 parser adapter.

Covers API kwargs contract, profile contract, preflight dependency checks,
visual enrichment deduplication, metrics serialization schema, and
native-Windows functional smoke.
"""
