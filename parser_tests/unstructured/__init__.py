"""Tests for the Unstructured v2 parser adapter.

Covers adapter contract, Markdown renderer, page grouping, page mapping,
OCR agent selection, per-page element counts, preflight checks, profile
contract, native serialization, and native-Windows functional smoke.
"""
