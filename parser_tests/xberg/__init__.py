"""Tests for the Xberg v2 parser adapter.

Covers adapter contract, API probe contract, config builder, layout config
keys, page content contract, preflight checks, profile contract, QR
extraction helpers, result validation, serialization schema, and
native-Windows functional smoke.
"""
